import { AnimateOnScrollDirectiveDirective } from './animate-on-scroll-directive.directive';

describe('AnimateOnScrollDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateOnScrollDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
